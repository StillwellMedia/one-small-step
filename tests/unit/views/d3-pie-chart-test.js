import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:d3-pie-chart', 'D3PieChartView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
