import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:goals/new', 'GoalsNewView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
